name: cipher_flutter
description: Material 3 Flutter migration of CipherJavaFX.
publish_to: "none"
version: 3.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cryptography: ^2.9.0
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2
  window_manager: ^0.5.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
